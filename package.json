{
  "name": "moa-small-business-funding",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "db:apply": "node scripts/apply_supabase.mjs",
    "test": "node --check src/app.js && node --check src/supabase-cloud.js && node --check src/submission-status.js && node --check api/ai.js && node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
