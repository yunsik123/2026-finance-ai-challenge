{
  "name": "moa-small-business-funding",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "db:apply": "node scripts/apply_supabase.mjs",
    "test": "node --check src/app.js && node --check src/supabase-cloud.js && echo 'All syntax checks passed.'"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
