# 먹투(Meoktu) 프로젝트 문서

> 좋아하는 동네 식당에 손님이 직접 돈을 넣고, 그 대가를 **현금 이자가 아니라 그 식당에서 쓰는 할인 쿠폰**으로 돌려받는 로컬 푸드 펀딩 플랫폼.
>
> 작성 기준일 2026-09-05 · 약관 버전 `2026-09-05.d2` · 상태: 기능 검증용 MVP(가상 머니·가상 식당)

이 문서는 **서비스가 무엇이고 어떻게 굴러가는지**를 정리한 프로젝트 개요다.
실행 방법과 환경변수는 [`README.md`](README.md), 제출용 상세 명세(204개 기능 항목)는 [`기능명세서.md`](기능명세서.md)에 있다.

---

## 1. 왜 만들었나

**소상공인 쪽 문제**
- 담보와 신용점수 위주의 여신 심사에서 매출이 나오는 가게도 자금을 못 받는다. 필요한 돈은 보통 2,000만~5,000만원 단위의 설비·리뉴얼 자금인데, 이 구간을 감당하는 상품이 마땅치 않다.
- 크라우드펀딩은 현금 이자·지분을 요구해 고정비가 되고, 지역 상권 데이터는 심사에 거의 반영되지 않는다.
- 심사 결과가 "왜 떨어졌는지" 설명되지 않는다.

**손님(투자자) 쪽 문제**
- 단골 가게가 사라지는 걸 지켜볼 뿐 관여할 방법이 없다.
- 소액으로 참여할 수 있는 로컬 투자 수단이 없고, 있어도 원금 회수 경로가 막혀 있다.

**먹투의 답**
- 대가를 **현금이 아니라 그 가게에서만 쓰이는 할인 쿠폰**으로 준다 → 사장님에게는 현금 유출이 아니라 **재방문 손님**이 되고, 투자자에게는 실제로 쓰는 혜택이 된다.
- 상환은 이자가 아니라 **쿠폰 사용분**으로 일어난다. 손님이 안 오면 부담도 안 생긴다.
- 심사는 점수만 던지지 않고 **어떤 자료로 어떻게 계산했는지, 무엇이 미산정인지**를 함께 공개한다.

---

## 2. 서비스 모델 — 돈과 혜택이 도는 구조

```
        ┌─────────── 투자자(단골 손님) ───────────┐
        │  먹투머니 충전 → 1,000원 단위 투자       │
        │  보유일수만큼 할인율 적립                 │
        │  10%p 넘으면 쿠폰 발급 → 매장에서 사용    │
        │  안 쓸 쿠폰은 교환장에서 다른 쿠폰과 교환  │
        └───────────────┬──────────────────────────┘
                        │ 자금
                        ▼
        ┌─────────── 소상공인(식당) ───────────────┐
        │  펀딩 신청 → 진위확인 → 자료 제출          │
        │  → AI 판독 → 6단계 교차검증 → 판정         │
        │  모금 성공 시 설비·리뉴얼에 사용            │
        │  쿠폰 사용 시점에만 할인 부담 발생          │
        └───────────────┬──────────────────────────┘
                        │ 재방문·매출
                        ▼
                  상권 데이터·매출 성장
                  → 매출 보너스로 투자자 적립률 상승
```

**핵심은 세 가지 비대칭을 없앤 것이다.**

| 기존 방식 | 먹투 |
| --- | --- |
| 이자 = 매출과 무관한 고정비 | 쿠폰 = 손님이 실제로 와야 발생하는 변동비 |
| 심사 근거 비공개 | 5요소 위험평가 + 35지표 예비평가의 **계산 근거와 미산정 지표까지 공개** |
| 회수 경로 없음 | 모금 중 즉시 회수 · 마감 후 1,000원 단위 FIFO 예약 매칭 |

---

## 3. 누가 쓰나 — 4가지 역할

| 역할 | 진입 | 할 수 있는 일 | 심사용 계정 |
| --- | --- | --- | --- |
| 투자자 | `/` → `/discover` | 탐색·투자·회수·쿠폰 발급/사용/교환·리뷰·문의 | `investor@meoktu.demo` / `demo1234!` |
| 소상공인(사장님) | 로그인 즉시 `/owner` | 펀딩 신청·자료 제출·심사 확인·쿠폰 확인·경영 진단 | `owner@meoktu.demo` / `demo1234!` |
| 관리자 | `/admin` | 회원·식당·펀드·심사·리뷰·문의·쿠폰 운영, AI 점검 | `admin@meoktu.demo` / `demo1234!` |
| 원클릭 체험 | 로그인창 하단 버튼 | 실제와 같은 버튼을 누르되 **기록은 4시간짜리 메모리 샌드박스에만** 남음 (투자자 체험은 30만원 지급) | 계정 없음 |

사장님 계정은 투자자 라우트로 접근하면 `/owner`로 리다이렉트된다. 역할은 요청 본문이 아니라 **로그인 토큰에서 매번 다시 확인**한다.

---

## 4. 화면 지도

| 경로 | 화면 | 핵심 내용 |
| --- | --- | --- |
| `/` | 홈 | 추천 식당, 진행 중 펀드 요약 |
| `/discover` | 식당 발견 | 지역·업종 필터, 카드 목록, 근처 지도, 찜 |
| `/market` | 거래장 | ① 쿠폰 교환장 ② 펀드 예약 거래(호가창) |
| `/my` | 마이페이지 | 잔액·투자 현황·누적 할인율·쿠폰 지갑·예약 주문·알림 |
| `/insight` | AI 인사이트 | 상담창, 식당 최대 3곳 비교, 상권·쿠폰 리포트 |
| `/owner` | 사장님 센터 | 신청·자료 제출·판독·교차검증·손익 대시보드·쿠폰 확인 |
| `/owner/my` | 사장님 마이페이지 | 신청 이력별 검증 리포트·등급·공개 상태 |
| `/admin` | 운영센터 | 통합 대시보드와 운영 액션 |
| `/support` | 신고·문의 | 1:1 문의 접수와 답변 확인 |
| `/legal`, `/legal/:documentId` | 약관·법적 고지 | 문서 9종, 버전·동의 이력 |

모든 화면 오른쪽 아래에 **AI와 상담하기** 플로팅 버튼이 붙는다.

---

## 5. 핵심 규칙 (전부 서버 한 곳에 정의되고 API로 공개된다)

### 5-1. 투자·회수

| 항목 | 값 |
| --- | --- |
| 거래 단위 | 투자·회수 모두 **1,000원** |
| 개인 한도 | 펀드 목표액의 **1%** (자체 투기 방지 규칙, 법정 한도를 대체하지 않음) |
| 모금 중 | 신청 금액 **전액 즉시 회수** — 매수자 불필요 |
| 예약 거래 중(마감 후) | 1,000원 단위 **시간우선 FIFO 단일가 매칭** |
| 미체결 주문 | 언제든 취소, 투자 예약 취소 시 먹투머니 즉시 환원 |
| 자기체결 | 차단 (같은 사람의 매수·매도 주문끼리는 체결되지 않음) |
| 보장 | 원금·회수 시점 모두 **미보장**, 예금자보호 대상 아님 |

> 마감된 펀드는 가격이 움직이지 않는다. 1,000원은 언제나 1,000원이고 **누가 먼저 줄을 섰는지**만 남는다. 그래서 가격·시간 우선 호가창이 아니라 시간 우선 단일가 대기열을 쓴다. 대기열은 익명이라 "누가 얼마"가 아니라 **"내 앞에 얼마"**만 보여준다.

### 5-2. 쿠폰 적립 공식

```
적립 할인율(%p) = (투자금 ÷ 100,000) × 0.5 × 보유일수 × (1 + 해당 펀드 매출 보너스 ÷ 100)
```

| 파라미터 | 값 |
| --- | --- |
| 기본 적립률 | 10만원당 **하루 0.5%p** (`dailyRatePer100k`) |
| 매출 보너스 | `min(35, 식당 매출성장률 × 0.8)` % — 펀드마다 다름 |
| 최초 투자자 보너스 | **매출 보너스 자체가 1.5배** (할인율이 1.5배가 되는 게 아님) |
| 발급 하한 | 누적 **10%p** 이상 (`minIssueDiscount`) |
| 발급 상한 | 펀드별 최대 할인율 **30~55%** (`maxDiscount`) |
| 발급 후 | 누적 할인율 0으로 초기화 후 재적립 |

예) 10만원을 최초 투자자로 30일 보유, 매출 보너스 14.7% → `0.5 × 30 × (1 + 0.147 × 1.5) ≈ 18.3%p`

### 5-3. 쿠폰 사용

지갑에서 **사용하기** → 8자리 코드 발급 → 사장님이 사장님 센터에서 코드 확인 → 사용 처리 및 펀드 누적 사용 혜택 반영.
**20분 안에 확인되지 않으면 자동으로 지갑에 복원**된다.

### 5-4. 쿠폰 교환 성립 조건 (`GET /api/market/rules`로 공개)

| 규칙 | 값 | 이유 |
| --- | --- | --- |
| 할인율 차이 | 10%p 미만 | 한쪽이 크게 손해 보는 교환 방지 |
| 최대 할인 금액 차이 | 2.5배 이내 | 같은 30%라도 2,880원짜리와 11,520원짜리는 값어치가 다름 |
| 만료 여유 | 7일 이상 | 곧 죽는 쿠폰 떠넘기기 방지 |
| 등록자 조건 | 업종·지역·최소 할인율 | 등록자가 지정한 조건을 **서버가 강제** |

흐름은 **등록(잠금) → 제안(에스크로) → 체결 → 기록 → 사용** 5단계다. 체결되면 같은 매물의 다른 제안은 자동 반려되고 걸려 있던 쿠폰이 모두 풀린다. 만료된 쿠폰·등록(30일)·제안(7일)은 조회·쓰기 직전마다 정리해 유령 매물이 남지 않는다.

---

## 6. 시연 원장에 들어 있는 것

식당 12곳 · 펀드 12개 · 테마 묶음 3개 · 상권 6곳.

| 식당 | 업종·지역 | 목표액 | 최대 할인 | 상태 | 자금 용도 |
| --- | --- | --- | --- | --- | --- |
| 소복소복 | 한식 · 서울 망원동 | 3,000만원 | 40% | 모금 중 | 저온 저장고·주방 동선 |
| 화향면관 | 중식 · 서울 연남동 | 4,500만원 | 50% | 모금 중 | 도삭면 제면실 확장 |
| 목화다방 | 카페 · 서울 성수동 | 2,000만원 | 35% | 예약 거래 | 소형 로스터·환기 설비 |
| 바다의 식탁 | 일식 · 부산 광안리 | 5,000만원 | 30% | 예약 거래 | 생선 숙성고·냉장 설비 |
| 오후의 오븐 | 베이커리 · 대전 소제동 | 1,800만원 | 45% | 모금 중 | 중고 데크오븐 |
| 녹턴 키친 | 양식 · 대구 동성로 | 2,500만원 | 40% | 모금 중 | 제면기·런치 운영 |
| 도토리분식 | 분식 · 서울 신림동 | 2,800만원 | 35% | 예약 거래 | 홀 리뉴얼·배달 의존도 개선 |
| 그린볼 클럽 | 샐러드 · 인천 송도동 | 2,200만원 | 45% | 모금 중 | 다회용기 세척 설비 |
| 포도상점 | 주점 · 광주 동명동 | 3,200만원 | 40% | 예약 거래 | 전통주 저온 저장고 |
| 밀밀키친 | 세계음식 · 서울 이태원동 | 3,600만원 | 55% | 모금 중 | 또르띠야 생산 장비 |
| 선만두 | 한식 · 수원 행궁동 | 4,000만원 | 30% | 예약 거래 | 수도권 당일 배송 냉장차 |
| 제주담 국수 | 한식 · 제주 노형동 | 4,200만원 | 45% | 모금 중 | 육수 HMR 생산 설비 |

**테마 묶음** — 마포 중식 한바퀴 / 동네빵 행복지수 / 밤의 식탁. 여러 식당을 묶어 분산해 보는 화면용 구성이다.

**상권 데이터 6곳** — 성수·연남·서촌·수원 행궁동·부산 전포·대전 은행동 (`data/commercial-areas.json`).
주소로 매칭하되 **짐작으로 붙이지 않는다.**

| 매칭 | 조건 | 데이터 신뢰도 |
| --- | --- | --- |
| exact | 동 이름이 상권 별칭과 일치 | 86 |
| nearby | 동 데이터는 없고 같은 시·구 (참고값) | 80 |
| 미연동 | 해당 없음 → 화면에 "상권 원천데이터 미연동" 표기 | 72 |

---

## 7. 심사 파이프라인

```
접수 ──▶ 사업자 진위확인 ──▶ 자료 수집 ──▶ AI 문서 판독 ──▶ 6단계 교차검증 ──▶ 판정
                                                                    │
                    승인 / 조건부 승인 / 수동 검토 / 보완 ◀──────────┘
```

### 7-1. 자료가 들어오는 두 경로 (출처를 분리해 기록)

| 경로 | 내용 | 기록 |
| --- | --- | --- |
| 소상공인 직접 업로드 | 사업자등록·영업신고·임대차·고객 가명 집계·직원 집계 | 파일명·크기·형식과 **CSV 열·행 수 검증** → `sourceProvenance.ownerUploaded` |
| 제휴기관(마이데이터형) 연결 | POS·사업용 계좌·카드/VAN·배달·세무·부채 | 제공기관·동의 범위·연결 시각·최근 동기화 시각 → `sourceProvenance.partnerConnected` |

브라우저가 임의로 보낸 `connectedSources` 문자열은 **제휴 자료로 인정하지 않는다.**

### 7-2. 6단계 재무 교차검증

| 단계 | 대조 대상 |
| --- | --- |
| 1 | 문서에서 읽은 사업자번호 ↔ 신고한 번호 |
| 2 | 판독 문서에 기준일자가 있는지 |
| 3 | POS·카드 문서 판독액 ↔ 산출 매출 |
| 4 | 부채 증빙 판독액 ↔ 신고 상환액 |
| 5 | 납세 자료 판독 판정 |
| 6 | 같은 내용 문서 중복 제출 여부 |

**불일치가 발견되면 점수와 무관하게 수동 심사로 넘어간다.** AI 판독은 보조자료이고, 통과로 표시돼도 운영자가 원본을 확인해야 공식 재무심사가 된다.

### 7-3. 두 개의 평가를 나란히 (평균 내지 않는다)

**① 5요소 투명 위험평가** — 기준점 60점에서 가감하는 가산형

| 구성요소 | 가중치 | 근거 |
| --- | --- | --- |
| 매출 지속성 | 25% | 매출 성장률, 재방문율 |
| 현금흐름 여력 | 25% | 매출 성장률, 모금 진척도 |
| 부채 부담 | 20% | 원자료 미연동 구간은 기준값 유지 후 **미산정으로 공개** |
| 사업 운영 안정성 | 15% | 업력, 운영 안정성 |
| 상권 회복력 | 15% | 유동인구 성장·상권 매출 성장·폐업률·경쟁밀도·임대료 상승 |

**② 35개 지표 · 6개 업종 먹투 성장성 예비평가** (`server/credit.ts`)

| 그룹 | 가중치 | 지표 수 |
| --- | --- | --- |
| 신용·부채 | 25% | 4 |
| 매출·거래 | 24% | 8 |
| 현금흐름 | 27% | 4 |
| 운영·상권 | 14% | 7 |
| 고객·평판 | 10% | 12 |

- 업종 6종(외식/도소매/숙박/제조/부동산/보건)마다 기준분포가 다르다. 하위10%·중앙·상위10% 세 점을 지나는 꺾은선으로 백분위를 근사하고, 낮을수록 좋은 지표는 뒤집는다.
- 등급: **A+ 85 / A 75 / B+ 65 / B 55 / C 45 / D**
- **정책 오버라이드**: 최대 연체일수 90일 이상이면 점수 무관 D. 측정 가중치 50% 미만이면 70점 상한.
- **미산정 지표는 0점 감점이 아니라 가중치에서 제외 후 재정규화**한다. 대신 산정률(coverage)이 떨어져 신뢰도로 드러난다.
- 산정률 50% 미만, 핵심 매출·현금흐름 미산정, 진위확인 실패, 자료 불일치 중 하나라도 있으면 **점수와 무관하게 수동 검토**.

두 평가는 보는 대상이 달라 평균 내지 않고 나란히 보여주며, 화면 종합값만 `combineAssessments()`로 섞는다(성장·상권 40% + 신용·현금흐름 60%).

> **이 결과는 먹투 성장성 예비평가다.** 금융기관의 공식 신용평가나 정부 SCB 결과가 아니고, 부도확률을 계산하지 않으며, 자동으로 거절하지 않는다.

---

## 8. AI가 하는 일 / 하지 않는 일

AI는 딱 두 군데 쓴다. **① 비정형 증빙 판독**과 **② 서비스 내부 데이터 기반 상담**.

### 8-1. 문서 판독(OCR)

- 사장님이 문서별 **AI 문서 판독** 버튼을 **직접 누른 경우에만** 호출된다.
- 사업자번호·상호·대표자·발급일자·매출금액·기준기간을 구조화 추출한다.
- 시스템 프롬프트에 *"보이는 값만 구조화하고 승인 여부를 결정하지 않는다"* 를 못 박아 모델이 판정에 개입하지 못하게 한다.
- **원본 이미지는 저장하지 않고** 구조화 결과와 바운딩박스, 감사 이력만 남긴다.
- 빈 결과·호출 실패 시 자동 승인하지 않고 `manual_review`로 기록한다.

### 8-2. AI 상담원 — 근거 5갈래

| # | 소스 | 역할 |
| --- | --- | --- |
| 1 | 화면 지도 (`server/sitemap.ts`) | "어디서 하나요"에 실제 메뉴명·버튼명·클릭 순서로 답. `currentPath`로 지금 화면도 안다 |
| 2 | 역할별 절차 그래프 (`server/trust.ts`) | 투자자/소상공인 각각의 확인 순서, 지금 보는 식당·펀드·평가 |
| 3 | 지원제도 KB (`server/knowledge.ts`) | 공적 지원제도 10종(정책자금·보증·노란우산·세제·두루누리·희망리턴 등) |
| 4 | 사장님 현재 상황 (`ownerSituation()`) | 요청 시점에 계산: 6단계 중 몇 번째, 확보·누락 자료, 불일치 건수, 다음 할 일 4가지 |
| 5 | 내 계정 원장 | 로그인한 사람의 실제 값 |

**역할에 따라 원장이 완전히 갈린다.**

| | 투자자 | 사장님 |
| --- | --- | --- |
| 원장 | 먹투머니 잔액, 투자 식당 수·금액, 쿠폰 상태별 장수, 예약 대기 금액, 교환 제안, 알림, 관심 식당 | 회차·모금 목표·모금액·투자자 수, 발급/사용 쿠폰 금액, **미사용 쿠폰 잠재 부담**, 확인 대기 쿠폰, 매출 공개 여부, 심사 결과, 내 문의 |

**하지 않는 것**
- 개인 원장 값을 **외부 생성형 서비스로 보내지 않는다.** 서버가 원장에서 직접 집계한 답을 그대로 돌려주고 `provider`는 `meoktu-private-ledger`가 된다.
- 투자자가 요청에 `role: 'owner'`를 위조해도 사장님의 심사·신용·제출자료 근거는 붙지 않는다.
- 화면과 답변 어디에도 **내부 용어(GraphRAG·지식그래프·노드·LLM·OCR·프롬프트·임베딩·벡터·토큰)를 노출하지 않는다.** 시스템 프롬프트에 금지어 목록이 있고, 감사 로그의 동작 코드도 한국어 라벨로 치환한다.
- AI 키가 없거나 외부 호출이 실패해도 같은 근거로 로컬 답변을 만든다(무응답 방지).

> **화면 문구를 바꾸면 `server/sitemap.ts`도 같이 고쳐야 한다.** 안 고치면 AI가 존재하지 않는 버튼을 안내한다.

---

## 9. 기술 구조

| 계층 | 구성 |
| --- | --- |
| 프론트 | React 18 + TypeScript + Vite, `react-router-dom`, `socket.io-client`, `lucide-react` |
| 백엔드 | Node.js + Express + Socket.IO — `server/index.ts` 단일 API 게이트웨이(약 55개 엔드포인트) |
| 데이터 | Supabase Postgres `app_state` 공유 원장 / 로컬 `data/db.json` (`STATE_STORE`로 전환) |
| 인증 | Supabase Auth 우선 + 로컬 폴백(scrypt 파생키 + HMAC-SHA256 서명 토큰, 유효기간 14일) |
| AI | OpenAI Chat Completions (기본 `gpt-4o-mini`) — 상담 모델과 OCR 모델 분리, 키는 서버 환경변수 전용 |
| 배포 | Vercel Serverless (`api/index.ts` 진입점, `vercel.json`이 `/api/*`만 함수로) |

### 9-1. 도메인 모듈 지도 — 뭘 고치려면 어딜 보나

| 파일 | 담당 |
| --- | --- |
| `server/index.ts` | API 게이트웨이, 인증, 투자·회수·FIFO 매칭, 쿠폰 발급/사용, AI 상담 조립 |
| `server/exchange.ts` | 쿠폰 교환 규칙·에스크로·동시성 잠금 (`withExchangeLock`) |
| `server/trust.ts` | 5요소 위험평가, 역할별 지식그래프 |
| `server/credit.ts` | 35지표 성장성 예비평가, 업종 프로필, 등급 경계 |
| `server/verification.ts` | 사업자 진위확인(국세청 검증번호 규칙), 6단계 교차검증 |
| `server/commercial.ts` | 주소 → 상권 매칭(exact/nearby/미연동) |
| `server/knowledge.ts` | 지원제도 10종 KB, `ownerSituation()` |
| `server/sitemap.ts` | 화면·메뉴·버튼 지도 (**화면 문구와 1:1로 유지**) |
| `server/legal.ts` | 약관 9종 본문, `LEGAL_VERSION`, 동의 필요 시점 |
| `server/demo.ts` | 체험 세션 메모리 샌드박스 |
| `server/seed.ts` | 시연 원장(식당 12·펀드 12·쿠폰·주문) |
| `server/store.ts` | 원장 읽기/쓰기와 공유 원장 잠금 |

**규칙은 화면이 아니라 서버 한 곳에만 정의하고 API로 공개한다.** 프론트는 안내만 하고, 최종 검증은 항상 체결 직전에 서버가 다시 한다.

### 9-2. 여러 사람이 동시에 쓰는 것을 전제로

| 대상 | 기법 |
| --- | --- |
| 공유 원장 쓰기 | `app_state` 행 **compare-and-set 잠금**. 잠금 안에서 최신 상태를 다시 읽고 핸들러 실행. 15초 이상 방치된 잠금은 자동 회수 |
| 공유 원장 읽기 | 버전 번호를 먼저 확인해 바뀌었을 때만 전체 재조회 |
| 쿠폰 교환 | 모든 교환 쓰기를 단일 큐로 직렬화하고 **체결 직전 조건 재검증**. 두 사람이 같은 매물을 동시에 집으면 정확히 한 명만 성공 |
| FIFO 매칭 | 생성 시각 정렬 후 순차 소진, 자기체결 차단, 부분체결 지원, 잔여 1,000원 미만은 주문 종료 |
| 실시간 반영 | 상태 변경 시 `io.emit('state:changed')` → 접속 중인 모든 브라우저 재조회 |

### 9-3. 남용 방지 (계정 또는 IP 슬라이딩 윈도, 회/분)

가입 20 · 로그인 IP 30 / 계정 8 · 교환 등록 20 · 교환 30 · 제안 30 · 사용확인 60 · 문서 판독 20 · AI 상담 20.
scrypt는 CPU 비용이 커서 **실패했을 때만 카운트를 올리는** peek/hit 분리 구조를 쓴다.

### 9-4. 보안·개인정보 처리

| 항목 | 처리 |
| --- | --- |
| 비밀번호 | scrypt 파생키(64byte) + 계정별 salt. 평문·가역 암호 미저장 |
| 세션 | HMAC-SHA256 서명 토큰. 토큰 수명이 14일이라 **계정 정지는 로그인 시점이 아니라 매 요청마다 재확인** |
| 증빙 이미지 | 원본 서버 미보관. 구조화 결과와 감사 이력만 저장 |
| 서비스 키 | `SUPABASE_SERVICE_ROLE_KEY`·`OPENAI_API_KEY`는 서버 환경변수 전용, `VITE_` 접두어 노출 금지 |
| 감사 이력 | 심사·운영·교환 체결 등 주요 쓰기마다 서버 감사 이벤트 기록 |

---

## 10. 약관과 동의 원장

문서는 코드 안에 버전과 함께 두고(`LEGAL_VERSION = '2026-09-05.d2'`), **어느 시점에 어떤 문서가 필수인지 문서 자체에 적어 둔다**(`requiredFor`).

| 문서 | 필수 시점 |
| --- | --- |
| 서비스 이용약관 | 가입 |
| 개인정보처리방침 | 가입 · 펀딩 신청 |
| 쿠폰 이용·환불·만료·양도 규정 | 가입 |
| 개인(신용)정보 처리·제공 동의서 | 펀딩 신청 |
| AI 자동화평가 설명 및 이의제기 절차 | 펀딩 신청 |
| 투자·펀딩 위험고지서 | 투자 |
| 식당 폐업·채무불이행 시 처리기준 | 투자 |
| 제휴 금융사업자와 먹투의 책임 범위 | 투자 |
| 민원·분쟁 처리절차 | 상시 열람 |

동의는 **가입·투자·회수·펀딩신청 시점마다** 문서 버전과 시각을 원장에 남긴다. 본문은 MVP 시연용 초안이며 실제 서비스 개시 전 변호사 검토가 필요하다(문서 안에도 같은 문구가 있다).

---

## 11. 어떻게 검증하나

```bash
npm run check             # tsc -b --force 타입 게이트 (증분 캐시 사고 방지로 --force 고정)
npm run test:integration  # 9개 스위트, SUPABASE_AUTH_DISABLED=1로 격리 실행
npm run test:ai           # AI 상담 30문항 채점 (외부 API 비용 발생 → 통합 테스트에서 분리)
npm run test:owner        # 사장님 검증 흐름
```

| 스위트 | 확인 범위 |
| --- | --- |
| `smoke.ts` | 공개 데이터, 로그인, 투자·실시간 반영, AI 답변, 심사, 깊은 링크 |
| `enhancements.ts` | 충전 단위, 매출 공개 정책, FIFO 자동매칭, 방문 인증 리뷰 |
| `integration-new.ts` | 위험모형, 역할 그래프, 찜 원장, 판독/수동검토, 감사 이력 |
| `coupon-cancel.ts` | 교환 취소 → 지갑 복원 → 재등록 |
| `coupon-exchange.ts` | 교환 조건 강제, 에스크로, 중복·자기교환 차단, **동시 교환 경합**, 사용 코드 |
| `merged-modules.ts` | 상권 매칭 3단계, 위험모형 연동, 진위확인, 재무 교차검증 |
| `demo-and-data.ts` | 체험 샌드박스 동작과 **공유 원장 무변경**, 세션 간 격리, 출처 분리 |
| `admin-operations.ts` | 관리자 권한 차단, 통합 대시보드, 운영 액션(변경 후 원복) |
| `ai-and-ui-audit.ts` | 화면 문구와 상담 지도 일치, 역할별 원장 분리, **역할 위장 시 비공개 심사 차단** |

AI 평가 하네스 30문항은 비로그인 10 · 투자자 10 · 사장님 10으로 나뉘고, **답변 숫자가 그 시점 DB 원장과 정확히 일치하는지**와 역할별 원장이 섞이지 않는지, 내부 용어가 새지 않는지를 함께 채점한다.

테스트용 별도 원장·포트를 쓰면 개발 중 데이터가 소진되지 않는다.

```bash
MEOKTU_DATA_DIR=.tmp/test-ledger MEOKTU_TEST_PORT=8799 npm run test:integration
```

---

## 12. 현재 상태

기능 항목 204개 기준 — **완료 191(93.6%) · 부분 완료 8(3.9%) · 미착수 5(2.5%)**. 화면 13종 · 세부 페이지 90종 · Epic 12종.

**🔶 부분 완료 — 구조는 완성, 실 연동만 남음**
먹투머니 충전(시연용) · 접근성 aria 속성 · 이메일 인증 · 대표자 본인인증 · 제휴기관 6종 연동 · 국세청 진위확인 API · 제휴 마이데이터 실연동 · AI 평가 이의제기 전용 폼

**⬜ 미착수 — 설계·문서만 완료**
비밀번호 재설정 · 소셜 로그인 · 정규화 Postgres 스키마와 트랜잭션 RPC 적용 · PG·예치금·정산 사업자 연동 · 푸시/이메일 알림

---

## 13. 실서비스로 가려면 남은 것

| 영역 | 필요한 것 |
| --- | --- |
| 신원·진위 | 사업자등록·영업신고·대표자 본인확인 제공사 (현재는 국세청 검증번호 규칙 기반 모의 검증. `verifyBusiness` 어댑터 교체만 하면 됨) |
| 매출 데이터 | 여신금융협회/카드사 또는 POS 사업자 제휴 (현재 제휴 연결 버튼은 시연 어댑터) |
| 공공 데이터 | 국세·지방세 납부와 행정처분 확인, 상권 유동인구·업종 매출·폐업률(KOSIS, 소상공인시장진흥공단) |
| 자금 | 결제·예치금·정산 사업자 연동 |
| 법률 | 전자금융·자본시장·유사수신 관련 검토, 약관·개인정보처리방침 변호사 검토, 금융당국 신고·등록 요건 확인 |
| 인프라 | 관리형 PostgreSQL 전환(`db/postgres-schema.sql`의 정규화 테이블·RLS·트랜잭션 RPC), 비밀번호 재설정·이메일 인증 |

> 현재 코드는 **기능 검증용 MVP**다. 실제 투자금 수취나 보관 기능을 켜기 전에 금융·소비자보호·개인정보 법률 검토가 반드시 필요하다.

---

## 14. 설계에서 지키기로 한 원칙

1. **규칙은 서버 한 곳에만.** 화면은 안내만 하고 최종 검증은 항상 체결 직전 서버에서 다시 한다.
2. **모르는 건 모른다고 표시한다.** 상권 데이터가 없으면 짐작으로 붙이지 않고 "미연동"으로 적고 신뢰도를 낮춘다. 미산정 지표는 0점 감점이 아니라 가중치에서 뺀다.
3. **AI는 판정하지 않는다.** 판독은 값만 뽑고, 불일치가 있으면 점수와 무관하게 사람에게 넘긴다. 자동 거절은 설계상 금지다.
4. **역할 원장은 섞지 않는다.** 역할은 요청 본문이 아니라 토큰에서 확인하고, 개인 원장 값은 외부 생성형 서비스로 보내지 않는다.
5. **사용자에게 내부 용어를 보여주지 않는다.** 소상공인과 일반 손님이 쓰는 화면이다.
6. **동시 사용을 기본값으로 가정한다.** 두 사람이 같은 매물을 동시에 집으면 정확히 한 명만 성공해야 하고, 그걸 테스트로 검증한다.
7. **체험 모드는 막는 모드가 아니다.** 실제와 같은 버튼을 누르고 같은 결과를 보되, 기록은 자기 샌드박스에만 남는다.
