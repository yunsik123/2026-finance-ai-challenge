# MOA 기능명세서

> 기준일: 2026-08-30  
> 대상: 현재 저장소의 실행 가능한 웹 프로토타입  
> 상태 표기: `구현`은 코드와 DB 흐름이 존재한다는 뜻이며, 실제 금융·결제·정부기관 연동을 뜻하지 않는다.

## 1. 제품 범위와 사실 경계

MOA는 소상공인이 사업·재무·상권·모집계획과 증빙을 제출하고, 운영자가 이를 검토하며, 투자자가 쿠폰형 혜택을 전제로 참여 흐름을 시험하는 제품 프로토타입이다.

- 실제 구현: Supabase 저장/RLS/RPC, 제출·검토 상태, OCR 보조 판독, 설명 가능한 예비평가, 모집/마일스톤/쿠폰 원장, 지도·QR·감사보고서 UI
- 가정 데이터: `src/demo-campaigns.js`의 6개 모집과 `commercial_area/commercial_data.json`의 상권 예시. 화면에서 예시·가상 검토용임을 표시한다.
- 미연결: 국세청·식품안전·MyData·은행·PG·실제 에스크로·POS·결제단말·문자/메일·법정 전자서명
- 금지 표현: 미연결 상태에서 “진위확인 완료”, “송금 완료”, “실제 결제”, “공식 신용평가”, “부도확률”, “SHAP 값”으로 표시하지 않는다.

## 2. 역할별 주요 흐름

### 2.1 소상공인

1. 로그인 후 사업체 기본정보를 등록한다.
2. 6개월 매출, 현금흐름, 부채/상환, 연체, 납세, 고용, 상권지표를 입력한다.
3. 예비평가의 총점·등급·요인점수·가중 기여도·누락항목을 확인한다.
4. 재무 원자료 이미지를 제출해 OCR 교차검증을 요청한다.
5. 모집 목적·금액·위험·마일스톤·쿠폰정책을 등록하고 심사를 요청한다.
6. 승인 후 진행상황, 증빙, 쿠폰 비용 원장을 확인한다.

### 2.2 투자자

1. 공개된 실제 DB 모집과 명시된 예시 모집을 구분해 탐색한다.
2. 카드 또는 상권 지도에서 사업·평가·상권·위험·마일스톤을 검토한다.
3. 금액·위험고지를 확인하고 참여/회수 흐름을 실행한다.
4. 쿠폰 지갑에서 QR 식별코드를 열고 주문금액 기준 사용 결과를 시뮬레이션한다.

### 2.3 운영자

1. 사업자/재무/OCR 결과와 불일치·누락을 검토한다.
2. 모집계획과 평가한도를 검토해 승인·보완·반려한다.
3. 증빙 원문과 OCR 박스를 대조하고 지급단계를 승인한다.
4. 한 번의 클릭으로 해당 모집의 종합 심사·감사 보고서를 미리보기/저장한다.

## 3. 첨부 피드백 반영 기능

### F-01 OCR 바운딩 박스 시각화 — 구현

- 입력: OCR 응답의 `boundingBoxes[]`
- 좌표: 이미지 좌상단 기준 0~1000 정규화 `[x, y, width, height]`
- 검증: 최대 24개, 숫자가 아닌 좌표/0 이하 크기 제거, 이미지 범위로 보정, 신뢰도 0~1 보정
- 표시: 원본 이미지 위에 필드명·판독값과 상태별 색상(일치/검토/불일치)을 겹쳐 표시
- 적용 화면: 소상공인의 재무문서 즉시 결과, 지급증빙 OCR 즉시 결과
- 한계: 현재 원본 이미지는 브라우저 세션에만 남는다. 운영자 재열람을 위해서는 private Storage와 보존정책이 추가되어야 한다.

수용 기준:

- 범위 밖 좌표가 레이아웃을 넘지 않는다.
- 박스가 없는 응답도 결과표는 정상 렌더링한다.
- OCR 박스만으로 승인 상태가 바뀌지 않는다.

### F-02 설명 가능한 기여도 워터폴 — 구현

- 현재 모델: `moa-risk-v4-research-heuristic`
- 유형: `transparent_additive_prescreen`
- 기준점: 60점
- 계산: `최종점수 = 60 + Σ((요인점수 - 60) × 요인가중치)`

| 요인 | 가중치 | 현재 입력 |
|---|---:|---|
| 매출 지속성 | 25% | 6개월 성장률, 월매출 변동계수 |
| 현금흐름 여력 | 25% | 영업현금흐름/월 상환액, 영업현금흐름/평균매출 |
| 부채 부담 | 20% | 총부채/연매출, 연체 건수 |
| 사업 운영 안정성 | 15% | 업력, 납세, 행정처분, 대표자 변경 |
| 상권 회복력 | 15% | 유동인구 증감, 상권매출 증감, 주변 폐업률 |

- 경쟁밀도는 U자형·상권유형별 효과가 확인된 연구 때문에 현재 총점에서 제외하고 문맥 경고로만 표시한다.
- 저장: `credit_assessments.components`, `contributions`, `model_inputs`, `methodology`, `model_version`
- 표시: 모집 상세와 소상공인 결과의 양/음 기여 막대
- 문구: 학습모델의 SHAP가 아니라 현재 가중합의 정확한 분해임을 명시한다.

수용 기준:

- 저장된 기여도의 합과 기준점으로 최종점수가 재구성된다.
- 총점을 부도확률 또는 승인확률로 표현하지 않는다.
- 미검증 예비평가와 운영자 승인 평가가 구분된다.

### F-03 상권 지도와 쿠폰 QR 사용 시뮬레이터 — 구현

지도:

- Leaflet + OpenStreetMap 타일 사용
- 검색어와 공개상태를 카드/지도에 동일하게 적용
- 매장 정밀좌표가 없으면 `commercial_data.json`의 상권 중심점 사용
- 팝업에서 모집 상세 열기
- 상권 중심점임을 지도 아래에 명시

쿠폰 QR:

- QR payload: `{ type: "MOA_COUPON_V1", couponId, campaignId }`
- 주문금액을 입력하면 기존 `use_coupon` RPC가 소유자·상태·유효기간을 검사하고 할인액/사용시각을 원장에 기록
- 재사용은 DB 상태로 차단
- 투자자 화면 자체에서 실행하는 시뮬레이션이며 매장 POS나 실제 결제가 아님을 명시

수용 기준:

- 지도 전환 후 마커가 보이고 검색 결과와 일치한다.
- QR은 쿠폰마다 식별 가능한 실제 이미지로 렌더링된다.
- 이미 사용한 쿠폰은 다시 처리되지 않는다.

보안 후속:

- 실제 매장 사용 전 서명된 단기 토큰, nonce, 가맹점 권한, 서버측 스캔 API, 취소/분쟁 흐름이 필요하다.

### F-04 원클릭 종합 심사·감사 보고서 — 구현

- 진입: 운영자 모집 심사 카드의 보고서 버튼
- 데이터: 해당 캠페인, 사업체, 저장된 평가, 재무검증, 마일스톤, 해당 캠페인의 증빙, 관련 감사로그만 사용
- 출력: 화면 미리보기, 독립 HTML 다운로드, 브라우저 인쇄/PDF 저장
- 포함: 공식/예비 상태, 모델 버전, 요인점수/기여도, OCR 문서 수/평균 신뢰도, 불일치/누락, 사용계획/위험, 마일스톤/증빙
- 제외: 존재하지 않는 담당자 서명, 기관 인증, AI 생성 사실, 자동 승인 문구

수용 기준:

- 다른 캠페인의 증빙이 섞이지 않는다.
- HTML 특수문자를 이스케이프한다.
- 저장 자료가 없으면 “없음/미제출”로 표시하고 값을 만들지 않는다.

## 4. 기존 핵심 기능

| ID | 기능 | 현재 상태 | 실제 연동 경계 |
|---|---|---|---|
| C-01 | 역할별 Auth/RLS | 구현 | Supabase Auth 사용 |
| C-02 | 사업체 등록·검증 어댑터 | 구현 | 검증기는 `mock-v1`; 정부 API 미연결 |
| C-03 | 재무입력·예비평가 | 구현 | 입력값 기반 휴리스틱; 공식 신용평가 아님 |
| C-04 | 재무문서 다중 OCR 교차검증 | 구현 | Vision API 사용 가능; 원본 영구보관 미연결 |
| C-05 | 모집안/마일스톤 심사 | 구현 | DB 상태머신·원장; 실제 자금이동 없음 |
| C-06 | 투자·회수·FIFO 매칭 | 구현 | DB 원자성 시뮬레이션; PG/은행 미연결 |
| C-07 | 쿠폰 누적·발급·사용·교환 | 구현 | DB 쿠폰 원장; POS/정산 미연결 |
| C-08 | AI 상담 | 구현 | 저장 상태 기반 검토 보조; 최종 판단 금지 |
| C-09 | 근거 기반 랭킹/테마 | 구현 | 저장된 모집과 예시 데이터를 구분 |

## 5. 데이터 출처·신선도 규칙

모든 외부/대체 데이터에는 아래 메타데이터가 필요하다.

- `source_type`: owner_input, uploaded_document, government_api, mydata, bank_transaction, public_area_snapshot
- `source_name`: 제공기관/어댑터 이름
- `observed_at`: 실제 관측일
- `fetched_at`: 수집일
- `consent_id`: 개인/거래 데이터 동의 근거
- `verification_status`: unverified, matched, mismatch, operator_approved

현재 예시 상권 데이터는 정부·지자체 원자료와 비슷한 필드 구조를 가정한 샘플이며 실제 관측값으로 오인하지 않는다. 실서비스에서는 소상공인시장진흥공단, 서울 열린데이터광장, 국세청, 식품안전나라, 금융 MyData 등 계약·정책에 맞는 공식 소스로 교체한다.

## 6. 의도적으로 구현하지 않은 기능

- 학습 라벨 없이 만든 부도확률/AUC/정확도
- 가상의 심리검사 점수와 자동 거절
- 공급업체 관계가 없는 그래프 신경망 위험전염 점수
- 수집하지 않은 리뷰의 평점·측면감성·분산
- AI가 만든 사장님 이력, 메뉴명, 가격, 인증, 매출
- 임의 회사명·전화번호·사업자번호·통신판매 신고·보안 인증

기존 AI 스토리/메뉴 자동생성 버튼은 제거했다. 호환 API가 호출되더라도 사업자가 준 상호·업종·지역·키워드를 사실형 문장으로 정리할 뿐 사장님 이야기, 메뉴, 가격을 생성하지 않는다.

## 7. 테스트와 운영 확인

자동 검증:

- `npm test`: 문법, 위험식 재구성, 매출 변동성, OCR 좌표/교차검증, 감사보고서 필터링, 사실형 문구 생성
- `npm run build`: Vite production bundle
- `npm run check`: 위 두 작업 일괄 실행

배포 전 추가 확인:

1. 실제 Supabase에 `contributions`, `model_inputs`, `methodology` 컬럼 적용
2. 투자자 카드/지도 전환, 캠페인 상세 워터폴 확인
3. 소상공인 문서 업로드 직후 OCR 박스 확인
4. 운영자 보고서 미리보기/HTML/인쇄 확인
5. 쿠폰 QR 1회 사용과 재사용 차단 확인
6. 모바일 폭에서 지도·보고서·OCR 오버레이 확인

## 8. 다음 개발 우선순위

1. private Storage, 악성파일 검사, SHA-256 중복검사, 보존·파기 정책
2. 서명형 가맹점 QR 스캔 API와 POS/결제/취소·분쟁 원장
3. 정부 사업자·영업신고 진위조회 및 MyData 현금흐름 커넥터
4. 실제 관측 상권 시계열과 기준금리 스트레스 시나리오
5. 시간 순서 학습/검증 데이터셋, 로지스틱 기준모형, 비용민감 임계값, 공정성·안정성 모니터링
6. 충분한 데이터가 생긴 뒤 리뷰/공급망 모형을 별도 실험으로 평가

논문별 상세 근거와 보류 조건은 [docs/PAPER_APPLICATIONS.md](docs/PAPER_APPLICATIONS.md)를 참조한다.
